(() => {
'use strict';
if(window.__RRN_TENANT_BRANDING_RUNTIME__)return;window.__RRN_TENANT_BRANDING_RUNTIME__=true;
const cfg=window.RRN_SUPABASE||{};if(!window.supabase?.createClient||!cfg.url||!cfg.anonKey)return;
const client=window.RRN_SUPABASE_CLIENT||window.supabase.createClient(cfg.url,cfg.anonKey,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});
function applyBrand(b={}){const r=document.documentElement.style;const map={primary_color:'--primary',secondary_color:'--secondary',accent_color:'--accent',surface_color:'--surface',text_color:'--text'};Object.entries(map).forEach(([k,v])=>{if(b[k])r.setProperty(v,b[k])});if(b.primary_color)r.setProperty('--blue',b.primary_color);if(b.secondary_color)r.setProperty('--lilac',b.secondary_color);if(b.accent_color)r.setProperty('--yellow',b.accent_color);if(b.surface_color)r.setProperty('--beige',b.surface_color);document.querySelectorAll('.brand-mark img,.mobile-brand img,.navbar-icon,.brand img').forEach(img=>{if(b.logo_url)img.src=b.logo_url});const title=document.getElementById('brandTitle');const subtitle=document.getElementById('brandSubtitle');if(title&&b.login_title)title.textContent=b.login_title;if(subtitle&&b.login_subtitle)subtitle.textContent=b.login_subtitle;const panel=document.querySelector('.brand-panel');if(panel&&b.login_background_url){const p=b.primary_color||'#163A4D',s=b.secondary_color||'#2F7D78';panel.style.backgroundImage=`linear-gradient(145deg,${p}e6,${s}bd),url('${b.login_background_url}')`;panel.style.backgroundSize='cover';panel.style.backgroundPosition='center'}if(b.tenant_name){document.querySelectorAll('[data-tenant-brand-name]').forEach(el=>el.textContent=b.tenant_name)}window.RRN_TENANT_BRANDING=b}
async function fromSlug(slug){const {data,error}=await client.rpc('get_public_tenant_branding',{p_slug:slug});if(error)throw error;const row=Array.isArray(data)?data[0]:data;if(row)applyBrand(row)}
async function fromSession(){const {data:{session}}=await client.auth.getSession();if(!session?.user)return;const {data:p}=await client.from('profiles').select('tenant_id').eq('user_id',session.user.id).maybeSingle();if(!p?.tenant_id)return;const {data:b}=await client.from('tenant_branding').select('*').eq('tenant_id',p.tenant_id).maybeSingle();if(b)applyBrand(b)}
(async()=>{try{const slug=new URLSearchParams(location.search).get('org');if(slug)await fromSlug(slug);else await fromSession()}catch(e){console.warn('RRN branding:',e)}})();
})();