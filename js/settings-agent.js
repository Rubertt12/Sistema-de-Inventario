(() => {
  'use strict';
  // Módulo legado desativado. A tela do Agente RRN é mantida exclusivamente
  // por /js/settings-agent-v2.js para evitar UI duplicada e loops de MutationObserver.
  window.__RRN_SETTINGS_AGENT__ = true;
})();
